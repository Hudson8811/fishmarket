mixin title(tag="h2", text="Заголовок", classes)
    case tag
        when 'h1'
            .text-block(class=classes)
                h1!= text
        when 'h2'
            .text-block(class=classes)
                h2!= text